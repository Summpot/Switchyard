<Project>

  <PropertyGroup Condition="'$(SwitchyardEnabled)' == 'true'">
    <SwitchyardTaskAssemblyDir Condition="'$(SwitchyardTaskAssemblyDir)' == ''">$(MSBuildThisFileDirectory)..\tasks\</SwitchyardTaskAssemblyDir>

    <SwitchyardTaskAssembly Condition="$(MSBuildRuntimeType) == 'Core'">$(SwitchyardTaskAssemblyDir)net8.0\Switchyard.Task.dll</SwitchyardTaskAssembly>
    <SwitchyardTaskAssembly Condition="$(MSBuildRuntimeType) != 'Core'">$(SwitchyardTaskAssemblyDir)net472\Switchyard.Task.dll</SwitchyardTaskAssembly>
  </PropertyGroup>

  <UsingTask TaskName="Switchyard.SwitchyardTask"
             AssemblyFile="$(SwitchyardTaskAssembly)"
             Condition="'$(SwitchyardEnabled)' == 'true'" />

  <UsingTask TaskName="Switchyard.SwitchyardDepsPatcherTask"
             AssemblyFile="$(SwitchyardTaskAssembly)"
             Condition="'$(SwitchyardEnabled)' == 'true'" />

  <UsingTask TaskName="Switchyard.SwitchyardPublishFilterTask"
             AssemblyFile="$(SwitchyardTaskAssembly)"
             Condition="'$(SwitchyardEnabled)' == 'true'" />

  <UsingTask TaskName="Switchyard.SwitchyardNativeAotDirectPInvokeFilterTask"
             AssemblyFile="$(SwitchyardTaskAssembly)"
             Condition="'$(SwitchyardEnabled)' == 'true'" />

  <!--
    Phase 1: weave/redirect.

    Runs before CopyFilesToOutputDirectory so the rewritten caller assemblies
    and the renamed target assemblies replace the originals in the copy-local
    stream. Must run after CoreCompile so @(IntermediateAssembly) exists.
  -->
  <Target Name="ExecuteSwitchyardWeaving"
          BeforeTargets="CopyFilesToOutputDirectory;ComputeFilesToPublish"
          AfterTargets="CoreCompile"
          Condition="'$(SwitchyardEnabled)' == 'true' And '$(DesignTimeBuild)' != 'true'">

    <PropertyGroup>
      <SwitchyardTargetFramework Condition="'$(SwitchyardTargetFramework)' == ''">$(TargetFramework)</SwitchyardTargetFramework>
    </PropertyGroup>

    <SwitchyardTask
        PackageReferences="@(PackageReference)"
        ReferenceCopyLocalPaths="@(ReferenceCopyLocalPaths)"
        ProjectAssetsFile="$(ProjectAssetsFile)"
        IntermediateOutputPath="$(IntermediateOutputPath)"
        TargetFramework="$(SwitchyardTargetFramework)"
        AssemblyName="$(AssemblyName)"
        IntermediateAssembly="@(IntermediateAssembly)"
        NativeAotDirectPInvokeMode="$(SwitchyardNativeAotDirectPInvokeMode)"
        Silent="$(SwitchyardSilent)">

      <Output TaskParameter="NewReferenceCopyLocalPaths"
              ItemName="SwitchyardNewReferenceCopyLocalPaths" />
      <Output TaskParameter="NewIntermediateAssembly"
              PropertyName="SwitchyardNewIntermediateAssemblyPath" />
      <Output TaskParameter="NewRoutedAssemblies"
              ItemName="SwitchyardRoutedAssemblies" />
      <Output TaskParameter="NewRemovedOriginalNativePaths"
              ItemName="SwitchyardRemovedOriginalNativePaths" />
      <Output TaskParameter="NewRoutedNativeLibraries"
              ItemName="SwitchyardRoutedNativeLibraries" />
      <Output TaskParameter="NewStrippedOriginalPackageIds"
              ItemName="SwitchyardStrippedOriginalPackageIds" />
    </SwitchyardTask>

    <ItemGroup>
      <ReferenceCopyLocalPaths Remove="@(ReferenceCopyLocalPaths)" />
      <ReferenceCopyLocalPaths Include="@(SwitchyardNewReferenceCopyLocalPaths)" />
    </ItemGroup>

    <ItemGroup Condition="'$(SwitchyardNewIntermediateAssemblyPath)' != '' And '$(SwitchyardNewIntermediateAssemblyPath)' != '@(IntermediateAssembly->'%(FullPath)')'">
      <IntermediateAssembly Remove="@(IntermediateAssembly)" />
      <IntermediateAssembly Include="$(SwitchyardNewIntermediateAssemblyPath)" />
    </ItemGroup>

    <ItemGroup>
      <FileWrites Include="$(IntermediateOutputPath)switchyard\**" />
    </ItemGroup>

  </Target>

  <!--
    NativeAOT direct P/Invoke shaping.

    The normal Switchyard pipeline replaces @(IntermediateAssembly) for the
    SDK's build/publish file streams, but NativeAOT computes its own
    @(ManagedBinary) / @(IlcCompileInput) items before writing the ilc response
    file. Keep those compiler inputs pointed at the rewritten main assembly so
    ilc sees the same routed references the runtime would see.

    The native-isolation path also rewrites each routed managed assembly's
    DllImport module name to a version-specific native library name, e.g.
    libSkiaSharp.Switchyard.2.88.9. NativeAOT's lazy P/Invoke path can consume
    those rewritten names directly. Direct P/Invoke is only enabled
    automatically for routed native modules whose imported entry point names
    are unique; two versions of the same native package normally export the
    same symbols, and the native linker cannot disambiguate those symbols by
    module name alone.
  -->
  <Target Name="SwitchyardNativeAotCompileInputs"
          BeforeTargets="WriteIlcRspFileForCompilation"
          Condition="'$(SwitchyardEnabled)' == 'true'
                     And '$(DesignTimeBuild)' != 'true'
                     And '$(PublishAot)' == 'true'
                     And ('$(SwitchyardNewIntermediateAssemblyPath)' != ''
                          Or '@(SwitchyardRoutedNativeLibraries)' != '')">

    <SwitchyardNativeAotDirectPInvokeFilterTask
        RoutedNativeLibraries="@(SwitchyardRoutedNativeLibraries)"
        Condition="'@(SwitchyardRoutedNativeLibraries)' != ''">
      <Output TaskParameter="SafeDirectPInvokeLibraries"
              ItemName="_SwitchyardSafeDirectPInvokeLibraries" />
    </SwitchyardNativeAotDirectPInvokeFilterTask>

    <ItemGroup>
      <IlcCompileInput Remove="@(ManagedBinary)"
                       Condition="'$(SwitchyardNewIntermediateAssemblyPath)' != ''" />
      <ManagedBinary Remove="@(ManagedBinary)"
                     Condition="'$(SwitchyardNewIntermediateAssemblyPath)' != ''" />
      <ManagedBinary Include="$(SwitchyardNewIntermediateAssemblyPath)"
                     Condition="'$(SwitchyardNewIntermediateAssemblyPath)' != ''" />
      <IlcCompileInput Include="@(ManagedBinary)"
                       Condition="'$(SwitchyardNewIntermediateAssemblyPath)' != ''" />

      <_SwitchyardOriginalRoutedPackageReference
          Include="$(OutDir)%(SwitchyardRoutedAssemblies.Identity).dll"
          Condition="'@(SwitchyardRoutedAssemblies)' != ''
                     And !Exists('$(IntermediateOutputPath)switchyard\%(SwitchyardRoutedAssemblies.Identity).Switchyard.%(SwitchyardRoutedAssemblies.RoutedVersion).dll')
                     And Exists('$(OutDir)%(SwitchyardRoutedAssemblies.Identity).dll')" />
      <IlcReference Include="@(_SwitchyardOriginalRoutedPackageReference)" />

      <_SwitchyardNativeAotModule Include="@(_SwitchyardSafeDirectPInvokeLibraries->'%(ModuleName)')"
                                  Condition="'%(_SwitchyardSafeDirectPInvokeLibraries.ModuleName)' != ''" />
      <_SwitchyardNativeAotLinkerInput Include="@(_SwitchyardSafeDirectPInvokeLibraries->'%(LinkerPath)')"
                                       Condition="'%(_SwitchyardSafeDirectPInvokeLibraries.LinkerPath)' != ''" />

      <DirectPInvoke Include="@(_SwitchyardNativeAotModule)" />
      <NativeLibrary Include="@(_SwitchyardNativeAotLinkerInput)" />
    </ItemGroup>

    <Message Text="Switchyard: NativeAOT compile input uses $(SwitchyardNewIntermediateAssemblyPath)"
             Condition="'$(SwitchyardSilent)' != 'true' And '$(SwitchyardNewIntermediateAssemblyPath)' != ''"
             Importance="high" />
    <Message Text="Switchyard: NativeAOT direct P/Invoke includes @(_SwitchyardNativeAotModule, ' ')"
             Condition="'$(SwitchyardSilent)' != 'true' And '@(_SwitchyardNativeAotModule)' != ''"
             Importance="high" />
    <Message Text="Switchyard: NativeAOT direct P/Invoke skipped routed native libraries with duplicate entry point names; lazy P/Invoke will bind their routed module names at runtime."
             Condition="'$(SwitchyardSilent)' != 'true'
                        And '@(SwitchyardRoutedNativeLibraries)' != ''
                        And '@(_SwitchyardNativeAotModule)' == ''"
             Importance="high" />
  </Target>

  <!--
    Phase 2: deps.json shaping.
  -->
  <Target Name="PatchSwitchyardDepsJson"
          AfterTargets="GenerateBuildDependencyFile;GeneratePublishDependencyFile"
          BeforeTargets="ComputeFilesToPublish"
          Condition="'$(SwitchyardEnabled)' == 'true' And '$(DesignTimeBuild)' != 'true' And '@(SwitchyardRoutedAssemblies)' != ''">

    <SwitchyardDepsPatcherTask
        DepsFilePath="$(ProjectDepsFilePath)"
        RoutedAssemblies="@(SwitchyardRoutedAssemblies)"
        StrippedOriginalPackageIds="@(SwitchyardStrippedOriginalPackageIds)"
        Silent="$(SwitchyardSilent)" />

  </Target>

  <!--
    Phase 3: publish copy-local cleanup.

    The publish pipeline re-resolves copy-local assets from the lock file
    (ResolveLockFileCopyLocalFiles / _ResolvedCopyLocalBuildAssets), which
    resurrects the original un-routed package DLLs (e.g. TargetLib.dll) and
    native libraries (e.g. libskiasharp.dll) into the publish file stream
    even though the build phase removed them from @(ReferenceCopyLocalPaths).
    This target runs right before the publish asset list is finalised and
    strips only the original package DLLs that Phase 1 proved are unused (by
    package id), plus the original native libraries (by file name), back out of
    both @(ReferenceCopyLocalPaths) and @(_ResolvedCopyLocalPublishAssets). An
    original package version that is still the binding target for some caller
    must remain available, e.g. the app itself using SkiaSharp 4.x while
    Avalonia is routed to SkiaSharp 2.x.
  -->
  <Target Name="SwitchyardPublishCleanup"
          BeforeTargets="_ComputeResolvedCopyLocalPublishAssets"
          Condition="'$(SwitchyardEnabled)' == 'true' And '@(SwitchyardRoutedAssemblies)' != ''">

    <SwitchyardPublishFilterTask CopyLocalItems="@(ReferenceCopyLocalPaths)"
                                 RoutedAssemblies="@(SwitchyardStrippedOriginalPackageIds)"
                                 BlockedNativeFileNames="@(SwitchyardRemovedOriginalNativePaths)">
      <Output TaskParameter="FilteredItems" ItemName="_SwitchyardFilteredRefCopyLocal" />
    </SwitchyardPublishFilterTask>
    <SwitchyardPublishFilterTask CopyLocalItems="@(_ResolvedCopyLocalPublishAssets)"
                                 RoutedAssemblies="@(SwitchyardStrippedOriginalPackageIds)"
                                 BlockedNativeFileNames="@(SwitchyardRemovedOriginalNativePaths)">
      <Output TaskParameter="FilteredItems" ItemName="_SwitchyardFilteredPublishAssets" />
    </SwitchyardPublishFilterTask>

    <ItemGroup>
      <ReferenceCopyLocalPaths Remove="@(ReferenceCopyLocalPaths)" />
      <ReferenceCopyLocalPaths Include="@(_SwitchyardFilteredRefCopyLocal)" />
      <_ResolvedCopyLocalPublishAssets Remove="@(_ResolvedCopyLocalPublishAssets)" />
      <_ResolvedCopyLocalPublishAssets Include="@(_SwitchyardFilteredPublishAssets)" />
    </ItemGroup>

    <Message Text="Switchyard: publish cleanup stripped original package DLLs for @(SwitchyardStrippedOriginalPackageIds->'%(Identity)', ' ')"
             Condition="'$(SwitchyardSilent)' != 'true'"
             Importance="high" />
  </Target>

</Project>
